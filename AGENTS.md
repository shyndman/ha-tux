You **MUST** type everything!

No Anys, no dicts for data storage (use TypedDict), use Pydantic at the edges

Home Assistant `core` source is checked out locally at `~/dev/github.com/home-assistant/core` — read it directly instead of guessing at HA behavior.
